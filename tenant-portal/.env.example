APP_NAME="Rusunawa Tenant Portal"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

SERVICES_API_URL=http://localhost:8001

SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

CACHE_DRIVER=file
FILESYSTEM_DISK=local

# Add other config values as needed
```

This Laravel application provides a tenant-focused frontend for your Golang backend API. It includes:

1. Authentication (login/register/password reset)
2. Profile management
3. Room browsing and booking
4. Document management
5. Payment processing
6. Issue reporting
7. Notification handling
8. Waiting list management

To complete the implementation, you would need to:

1. Create the necessary view files using Blade
2. Implement the remaining controllers
3. Add CSS styling (you might use a framework like Bootstrap or Tailwind)
4. Set up proper validation and error handling

This structure follows MVC patterns and uses services to abstract API calls, making the application maintainable and extensible.
